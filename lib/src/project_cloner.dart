import 'package:build_runner_benchmark/src/structure_config.dart';

class ProjectCloner{
  final String projectRoot;

  ProjectCloner(this.projectRoot);

  StructureConfig execute(){

    return null;
  }
}